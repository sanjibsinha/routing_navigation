import 'package:flutter/material.dart';

class CategoryTest extends StatelessWidget {
  const CategoryTest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Category Second Page'),
      ),
    );
  }
}
